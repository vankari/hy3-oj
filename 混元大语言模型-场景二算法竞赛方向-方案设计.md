# 腾讯混元大语言模型项目 · 场景二：算法竞赛方向 —— 方案设计文档

> 2026 腾讯犀牛鸟开源人才培养计划 · 开源课题实战（Shape with AI）
> 项目：腾讯混元大语言模型（Hy3） ｜ 场景二：算法竞赛方向

---

## 一、项目背景与目标

### 1.1 背景

腾讯混元 **Hy3** 是混元最新开源的大语言模型，采用**快慢思考融合的混合专家（MoE）架构**，总参数 295B、激活参数 21B，最大支持 **256K 上下文**，在**复杂推理、长上下文理解、代码生成、Agent 工具调用**等方向有显著提升，已基于 Apache 2.0 协议开源。

算法竞赛（ACM-ICPC / Codeforces / AtCoder 类题目）是检验大模型**复杂推理 + 代码生成 + 自我纠错**能力的天然试金石：题目有标准输入输出、可自动化判题、难度分级明确、社区数据丰富。围绕 Hy3 构建一套算法竞赛智能解题系统，既能充分展示模型能力，也能沉淀可复用的 Agent 工程范式与评测资产。

### 1.2 目标

构建 **Hy3-OJ**：一个基于 Hy3 API 的算法竞赛端到端智能体系统，实现（对齐任务书场景二要求，详见 [`docs/pdf任务书对齐.md`](docs/pdf任务书对齐.md)）：

1. **自动解题**：输入题面（自然语言描述 + 输入输出格式 + 样例），自动产出可通过评测的正确代码与**完整解题过程**（思路、复杂度分析、边界处理）；
2. **闭环自纠错**：代码在沙箱中执行，依据判题反馈（WA / TLE / RE / CE）自动定位原因并迭代修复；
3. **过程评估（任务书核心）**：对解题过程做分步审查——过程正确性判定（跳步/循环论证/误用定理/条件遗漏/幻觉）、错误步骤定位、错误类型归类，并识别**答案正确但过程不成立**的样本（蒙对检测）；评估器本身经定位准确率与误报率验证；
4. **科学评测**：在公开竞赛基准（CodeContests 优先，LiveCodeBench 后置）上给出 pass@1 / pass@k、过程正确率、错误类型分布、难度分层等量化指标，形成评测报告；
5. **开源交付**：代码、评测数据管线、技术报告全部开源（自建公开仓库），README 明确 Hy3 在系统中承担的角色，并标注为个人/活动作品。

### 1.3 约束

- 全程通过 **API 调用 Hy3**，不做训练 / 微调 / 本地推理部署；
- 鼓励使用 CodeBuddy / WorkBuddy 辅助开发（课题实战阶段有免费 token 额度）；
- 产出需包含可交互 demo、可复现的评测结果与技术报告。

---

## 二、设计思路

### 2.1 核心思路：「规划 — 生成 — 执行 — 反思」解题闭环 + 过程评估双轨

单次直接生成代码的 pass@1 上限明显，本方案的核心是用 **Agent 化的多轮闭环**逼近模型的能力上限：

```
题面输入 → 题意解析与规划（慢思考） → 多路代码生成（快思考采样）
        → 沙箱判题 → 通过？─ 是 → 输出答案
                       └ 否 → 错误归因（WA/TLE/RE/CE）→ 定向修复 → 回到沙箱判题（限 N 轮）
                               
解题轨迹（Plan/代码/判题/反思，全程落盘）→ 过程评估器（Reviewer）
        → 过程正确性判定 → 错误步骤定位 → 错误类型归类 → 蒙对检测（AC 但过程不成立）
```

**双轨关系**：闭环产生的完整轨迹（而非仅最终代码）正是任务书要求"过程评估"的天然输入；Reviewer 与 Reflector 平行——Reflector 面向"让答案变对"（结果层），Reviewer 面向"评判过程是否成立"（过程层），两者共享双层错误分类体系。

四个关键设计决策：

1. **快慢思考分工**：利用 Hy3 快慢思考融合特性——题意理解、算法设计、错误归因等"重推理"环节走慢思考（深度推理）；代码补全、格式改写等"轻量"环节走快思考，兼顾效果与成本/时延。
2. **Generate-and-Test 多路采样**：对同一题目采样 K 份候选解（不同算法思路提示：暴力/贪心/DP/数据结构/图论等），先在本地样例与自造测试用例上筛选，再提交判题，将 pass@k 转化为 pass@1。
3. **结构化错误归因**：判题反馈不只是"对错"，而是解析为 CE（编译错误）/ RE（运行时错误）/ WA（答案错误）/ TLE（超时）四类，每类对应不同的修复 prompt 策略（如 TLE → 要求分析复杂度并换更优算法；WA → 要求构造反例自查）。
4. **评测先行**：先搭评测管线与基线（单轮直出），再逐步叠加 Agent 模块，每个模块都用数据验证增益，避免"堆功能不见效"。

### 2.2 创新点

- **快慢思考自适应调度策略**：根据题目难度标签 / 历史失败轮数动态决定是否启用慢思考，形成"成本-效果"帕累托前沿分析；
- **测试用例自动生成器**：让 Hy3 依据题意自动生成边界测试用例（小数据对拍），在提交官方判题前过滤掉明显错误解，显著降低无效迭代；
- **算法标签路由**：先让模型预测题目算法标签（DP/图论/数论…），再挂载对应算法范式的 few-shot 提示，提升一胎命中率；
- **过程评估器与蒙对检测**：对解题轨迹做分步 LLM 审查 + 规则校验（硬编码样例/输入特判/声称复杂度 vs 实际实现比对），识别"答案正确但过程不成立"的样本；评估器以自动注入 bug 的 ground truth 验证定位准确率，以人工抽检验证误报率——这是本方案区别于一般刷题 Agent 的差异化亮点。

---

## 三、系统架构

> 本章给出总体方向；folder 级模块划分、各模块职责与交互关系、算法思路与参考文献详见项目专属文档 [`docs/项目架构设计.md`](docs/项目架构设计.md)。

### 3.1 总体架构图

```
┌────────────────────────────────────────────────────────────┐
│                      交互层（Demo / CLI）                     │
│        Web UI：题目提交 · 解题过程可视化 · 评测榜单展示          │
├────────────────────────────────────────────────────────────┤
│                      Agent 编排层（核心）                     │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────────┐  │
│  │ Parser    │ │ Planner  │ │ Coder    │ │ Reflector    │  │
│  │ 题意解析   │→│ 算法规划  │→│ 代码生成  │→│ 错误归因/修复 │  │
│  │ 标签路由   │ │ (慢思考) │ │(快思考×K)│ │ (慢思考)     │  │
│  └──────────┘ └──────────┘ └──────────┘ └──────┬───────┘  │
│  ┌──────────────────────────┐     反馈循环（≤N轮） │        │
│  │ Reviewer 过程评估器       │←──── 解题轨迹（落盘回放）│        │
│  │ 过程判定/定位/归类/蒙对检测 │                            │
│  └──────────────────────────┘                            │
├──────────────────────────┼───────────────────────────────┤
│                      执行与评测层                          │
│  ┌───────────────┐  ┌───────────────┐  ┌───────────────┐ │
│  │ 沙箱执行器      │  │ 测试用例生成器  │  │ 评测管线        │ │
│  │ 编译/运行/限资源 │  │ 边界用例+对拍  │  │ pass@1/pass@k │ │
│  └───────────────┘  └───────────────┘  └───────────────┘ │
├────────────────────────────────────────────────────────────┤
│                      模型接入层                              │
│        Hy3 API（快/慢思考模式）· 重试与限流 · token 计量      │
├────────────────────────────────────────────────────────────┤
│                      数据层                                  │
│   LiveCodeBench / CodeContests / Codeforces 题集 · 运行日志  │
└────────────────────────────────────────────────────────────┘
```

### 3.2 模块说明

| 模块 | 职责 | 关键设计 |
|---|---|---|
| **Parser** | 题面结构化：提取约束、输入输出格式、样例；预测算法标签 | 长题面利用 256K 上下文一次性读入；标签路由到算法范式提示库 |
| **Planner** | 产出解题思路：算法选型、复杂度分析、边界情况清单 | 慢思考模式；输出结构化 plan（JSON），供 Coder 消费 |
| **Coder** | 按 plan 生成可执行代码（先 Python3，稳定后扩展 C++17） | 快思考 K 路采样（K=4~8），温度分层；统一代码模板（读入/输出规范） |
| **Tester** | 生成边界测试用例 + 暴力对拍 | 对简单题自动生成 brute-force 参考解做差分测试 |
| **Executor** | 沙箱内编译运行，限时/限内存，返回判题状态 | 进程隔离 + rlimit；可平滑迁移到 CubeSandbox（犀牛鸟兄弟项目） |
| **Reflector** | 错误归因并生成修复指令 | 按 CE/RE/WA/TLE 四类策略修复；记录反思轨迹供报告分析 |
| **Reviewer** | 过程评估：分步审查解题轨迹，判定过程正确性、定位错误步骤、归类错误类型、蒙对检测 | 五段式审查（题意理解→算法选型→复杂度论证→边界处理→实现一致性）；规则校验（硬编码/特判/复杂度比对）+ 分步 LLM 审查混合 |
| **Evaluator** | 批量跑基准题集，计算 pass@1/pass@k、过程正确率、错误类型分布 | 无偏 pass@k 估计；难度分桶统计；定位准确率/误报率验证；结果落盘可复现 |

### 3.3 技术选型

| 层 | 选型 | 理由 |
|---|---|---|
| 模型 | Hy3 API（快/慢思考） | 课题指定；256K 上下文适合长题面 + 多轮轨迹 |
| 编排 | Python + asyncio（自研轻量编排，可选 tRPC-Agent） | 并发采样与判题；tRPC-Agent 为腾讯开源 Agent 框架，可加分 |
| 沙箱 | Docker SDK 一次性容器（容器内 rlimit 限资源；预留 CubeSandbox 适配接口） | 开发机为 Windows（宿主机 rlimit 不可用），隔离必须走容器；CubeSandbox 兼容 E2B SDK、60ms 建沙箱 |
| 前端 | Streamlit / Next.js 简单 Web UI | 快速交付可交互 demo |
| 评测 | **CodeContests 子集优先**（自带 public/private/generated 测试，本地判题完全可控），LiveCodeBench 后置接入（出可横向对比的榜单分数） | 社区公认基准，结果可横向对比 |

---

## 四、重点技术

1. **快慢思考调度**：按题目难度与迭代轮次动态切换 Hy3 思考模式，量化"推理深度-通过率-token 成本"三角关系，产出调度策略结论。
2. **Generate-and-Test 候选筛选**：K 路采样 + 样例/自造用例预筛 + 无偏 pass@k 评估，把采样多样性转化为稳定通过率。
3. **结构化自我修复（Reflexion 式）**：CE/RE/WA/TLE 四类反馈各自的修复 prompt 模板与最大轮数控制；TLE 触发复杂度重分析与算法替换，WA 触发反例构造。
4. **过程评估方法论（任务书核心）**：双层错误分类体系（结果层 CE/RE/WA/TLE × 过程层 题意误读/算法选型错误/复杂度误判/边界遗漏/实现逻辑错误/格式不符/幻觉）；五段式分步审查；蒙对检测规则集；评估器有效性验证（自动注入 bug 构造定位 ground truth → 定位准确率；AC 样本人工抽检 → 误报率）。
5. **测试用例自动生成与对拍**：LLM 生成边界数据 + brute-force 差分验证，降低无效提交，是竞赛场景特有的增效手段。
6. **沙箱安全执行**：Docker 容器内资源限制（CPU/内存/进程数/网络隔离）、超时强杀；先 Python3，C++17 编译链为 stretch goal。
7. **竞赛评测方法论**：pass@1 / 无偏 pass@k（组合估计）、按难度分桶、与公开榜单（LiveCodeBench leaderboard）横向对比。
8. **AI Coding 协作开发**：全程使用 CodeBuddy/WorkBuddy 辅助实现，README 记录 AI 协作的代码块与心得（契合本届"Open with AI"主题）。

---

## 五、预期效果

### 5.1 量化指标（在 200~400 题规模的评测子集上）

| 指标 | 基线（单轮直出） | 目标（完整系统） |
|---|---|---|
| Easy 档 pass@1 | 预期 60%+ | ≥ 80% |
| Medium 档 pass@1 | 预期 25%+ | ≥ 45% |
| Hard 档 pass@1 | 预期 < 10% | ≥ 15%（pass@8 ≥ 30%） |
| 平均迭代收敛轮数 | — | ≤ 4 轮 |
| 过程正确率（Reviewer 判定） | — | 给出与答案准确率的差距分析（答案对但过程不成立的比例） |
| 错误步骤定位准确率 | — | ≥ 70%（自动注入 bug 的 ground truth 上验证） |
| 过程评估误报率 | — | ≤ 20%（AC 样本人工抽检，~50 题） |
| 慢思考 token 成本 / 题 | — | 给出成本-效果帕累托曲线 |

> 注：基线数值将在第 2 周实测后校准，目标值按"闭环 + 多路采样"相对基线提升 1.5~2 倍设定。

### 5.2 交付物

1. **Hy3-OJ 开源仓库**（Apache 2.0，自建公开仓库）：完整解题 Agent + 过程评估模块 + 沙箱 + 评测管线；README 写明项目介绍/运行方式/环境要求，标注**个人/活动作品**与 Hy3 角色；API key 经环境变量传入，提供 `.env.example`；
2. **可交互 Web Demo**：提交题目 → 可视化解题轨迹（plan/代码/判题/反思/过程审查结论）→ 评测榜单，附 ≤2 分钟演示视频（展示一次完整解题 + 过程评估流程）；
3. **评测材料**：分层题集（来源/构造/分层依据说明）+ 标准答案 + 答案校验脚本 + 过程评估脚本；
4. **评测技术报告**：基线 vs 各模块消融（多路采样 / 对拍 / 反思轮数 / 快慢思考调度）；最终答案准确率 + 过程正确率 + 错误类型分布 + 难度分层与能力临界点分析；定位准确率/误报率验证数据及人工抽检记录；含失败案例分析；
5. **复现文档**：一键复现评测结果的脚本与数据说明；
6. **犀牛鸟结题材料**：技术分享文章 1 篇（知乎/CSDN），README 标注 Hy3 角色与 CodeBuddy 协作说明。

### 5.3 价值

- 对社区：提供一套可复用的"LLM × 算法竞赛"开源基线与评测资产；
- 对混元：在公开代码基准上形成 Hy3 能力的第三方验证与曝光；
- 对个人：完整经历 Agent 系统设计、沙箱工程、科学评测的开源协作全流程。

---

## 六、时间规划

对齐犀牛鸟课题实战节奏：**8 月 27 日提交本方案 → 9 月 11 日提交终稿（源码 + 可执行作品）**，有效开发窗口约 **16 天**，按 5 个阶段压缩排期：

| 日期 | 阶段 | 任务 | 里程碑 |
|---|---|---|---|
| D1–D3（8/27–8/29） | 环境·数据·基线 | 环境搭建（`hy3-oj`）；熟悉 Hy3 API（快/慢思考参数实测）；CodeContests 数据管线 + 难度分层抽样子集落盘；单轮直出基线 | 基线 pass@1 数据出炉，校准第五节目标值 |
| D4–D6（8/30–9/1） | 沙箱与判题 | Docker SDK 一次性容器执行器（Python3，限时/限内存/网络隔离）；CE/RE/WA/TLE 判题状态解析；与 CodeContests 官方 verdict 对拍校准 | 本地判题一致率 ≥ 99%，TLE 阈值定标 |
| D7–D10（9/2–9/5） | Agent 闭环 + 过程评估 | Parser/Planner/Coder/Reflector 四模块串联（≤N 轮修复）；K 路采样 + 测试用例对拍；**Reviewer 过程评估器**（五段式审查 + 蒙对检测规则）；双层错误分类体系 | 闭环版相对基线提升可测；Reviewer 在注入 bug 题集上跑通 |
| D11–D13（9/6–9/8） | 评测与 Demo | 子集全量评测 + 难度分桶 + 成本分析；**定位准确率/误报率验证 + 人工抽检**；LiveCodeBench 后置接入（时间允许则出分）；Streamlit Demo 与轨迹/过程审查可视化；演示视频 | Demo 可交互、报告初稿 |
| D14–D16（9/9–9/11） | 收尾交付 | 技术报告定稿、复现脚本与文档、仓库整理；9/11 提交终稿源码与可执行作品 | 全部交付 |

**并行策略**：评测管线（D4 起）与 Demo 骨架（D7 起）与主线并行推进；**D13 末冻结功能**，之后只修 bug 不加特性。

**风险与对策**：

- **API 额度/限流** → token 计量模块 + 缓存中间结果 + 评测子集分层抽样，先小集调试再大集跑分；
- **Hard 题提升不明显** → 诚实呈现，将 Hard 失败案例分析作为报告亮点（归因于算法洞察 vs 工程实现）；
- **时间紧张** → 评测管线与 Demo 并行推进，W4 末冻结功能，W5 起只修 bug 不加特性。

---

*本文档基于 2026 腾讯犀牛鸟开源人才培养计划「腾讯混元大语言模型」项目公开信息撰写，评测目标值为预估值，将在基线实测后校准。*

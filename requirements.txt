# Hy3-OJ 依赖清单（pip 钉版，配套 environment.yml 使用）
# 模型接入层：Hy3 API（OpenAI 兼容协议）
openai==1.55.3
httpx==0.27.2

# 基础设施：schema / 重试限流 / LLM 响应磁盘缓存
pydantic==2.9.2
tenacity==9.0.0
diskcache==5.6.3

# 数据层：CodeContests / LiveCodeBench 数据集加载与缓存
datasets==3.1.0
huggingface-hub==0.26.2
jsonlines==4.0.0

# 沙箱：Docker SDK（一次性容器判题，预留 CubeSandbox 适配）
docker==7.1.0

# 工具链：配置 / 日志 / 进度
pyyaml==6.0.2
rich==13.9.4
tqdm==4.67.1

# Demo（后置启用）与测试
streamlit==1.40.2
pytest==8.3.4

# 任务书 PDF 解析
pypdf==5.1.0
